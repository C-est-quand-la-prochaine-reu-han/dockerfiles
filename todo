environment variable tokens

pong CLI fix

counters (perfect hit, ball max speed, time stops game after 10m, hit count)

remote matches

1 vs 1 on the same computer
1 vs bot is buggy

matchmaking is fcked

winner are swapped in the front

env vars for hostname in front

prevent playing against bots in 2v2
